const Note = require("../models/Note");
const getDate = require("../scripts/date");

module.export = {
    //Write get, save, delete paths for notes so that users can add and delete notes on articles.
}