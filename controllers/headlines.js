const scrape = require("../scripts/scrape");
const date = require("../scripts/date");
const headline = require("../models/Headline");

module.export = {
    //write a function to pull in the date data as well as the scrape data and combine it so you can add it into the database.
    //Fetch, delete, update, get
}